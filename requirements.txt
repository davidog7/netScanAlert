# Requerimientos principales
click>=8.0.0          # Para la interfaz CLI
requests>=2.26.0      # Para las notificaciones de Telegram
python-telegram-bot>=13.7  # Opcional (alternativa más completa para Telegram)
python-dotenv>=0.19.0 # Para manejar configuraciones (opcional pero recomendado)
configparser>=5.0.2   # Para leer archivos de configuración

# Herramientas de red (dependencias para el escaneo)
scapy>=2.4.5          # Para escaneo ARP/ICMP (alternativa a arp-scan, requiere permisos de root)
python-nmap>=0.7.1    # Para detección de SO y escaneo avanzado (opcional)

# Utilidades
python-dateutil>=2.8.2 # Para manejo de fechas/horas
tqdm>=4.62.0          # Para barras de progreso (opcional)

# Para el formato CSV
pandas>=1.3.0         # Opcional (para manejo avanzado de CSV)

# Entorno de desarrollo (opcional)
flake8>=3.9.2         # Linter
black>=21.9b0         # Formateador de código